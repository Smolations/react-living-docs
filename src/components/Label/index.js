/**
 *  `Label`s are a multipurpose component. They can be used as tags, ribbons, and
 *  even form field labels.
 *
 *  @module Label
 */

// eslint-disable-next-line import/prefer-default-export
export { default as Label } from './Label';
